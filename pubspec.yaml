name: matchreport_v3
description: "A new Flutter project with MatchBook feature."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.15
  google_fonts: ^8.0.1


  pdf: ^3.11.0
  open_filex: ^4.4.0
  permission_handler: ^11.3.0
  intl: ^0.18.1
  printing: ^5.12.5

  #Fire base initilized
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.1.0
  google_sign_in: ^6.2.5
  sign_in_with_apple: ^6.1.0

  #  image_picker: ^1.1.2
  uuid: ^3.0.6
  shared_preferences: ^2.1.1
  sqflite: ^2.3.0
  path: ^1.9.0
  http: ^1.2.0
  image_picker: ^0.8.7+6
  supabase_flutter: 2.12.0
  flutter_slidable: ^3.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

 #Socal site
  url_launcher: ^6.2.5
  font_awesome_flutter: ^10.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/applogo.png
    - assets/logo.png
